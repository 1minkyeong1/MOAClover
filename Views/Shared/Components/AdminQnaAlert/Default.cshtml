@model int

<a asp-controller="Home"
   asp-action="QnAAdmin"
   asp-route-filter="waiting"
   class="admin-qna-link">
    Q&amp;A

    @if (Model > 0)
    {
        <span class="admin-qna-badge">@Model</span>
    }
</a>